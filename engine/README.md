# Engine source boundary

The exact, license-verified Stratagus revision will be staged at
`engine/stratagus/` from the immutable reference input. Do not build directly
inside `ref/`, and do not let CMake download a moving upstream branch.

